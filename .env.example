MCP_API_KEY=replace-with-a-long-random-secret
DASHBOARD_PASSWORD=replace-with-a-separate-read-only-secret
POSTGRES_PASSWORD=replace-with-a-database-password
DATABASE_URL=postgres://menoteam:replace-with-a-database-password@db:5432/menoteam
PORT=3000
BIND_ADDRESS=127.0.0.1
HOST=0.0.0.0
APP_VERSION=0.1.1
# Comma-separated allowlists. Leave unset only when the service is private-network-only.
ALLOWED_HOSTS=localhost,127.0.0.1
ALLOWED_ORIGINS=http://localhost:3000
