# Credential-safe Master preflight template. Copy to an ignored local file.
# Never commit a filled copy or paste bearer/Discord tokens into chat.
WORK_MAP_MCP_URL=https://team.example.net/mcp
WORK_MAP_MCP_API_KEY=read-this-from-your-secret-manager
WORK_MAP_HARNESS=hermes
WORK_MAP_ROLE=master
WORK_MAP_CHANNEL=discord
WORK_MAP_MASTER_MENTION=@Master

# Required only when the selected native harness binds a Discord channel.
DISCORD_CHANNEL_ID=replace-with-selected-discord-channel-id

# Hermes Discord policy (non-secret; omit if another harness owns the channel).
DISCORD_ALLOW_BOTS=mentions
DISCORD_BOTS_REQUIRE_INLINE_MENTION=true
DISCORD_AUTO_THREAD=false
DISCORD_REACTIONS=false
DISCORD_HISTORY_BACKFILL=false

# The Master is not a human owner, so it needs no WORK_MAP_TEAMMATE_REF or
# WORK_MAP_AGENT_ADDRESS. Keep native harness credentials in that harness's
# supported secret path; they do not belong in this template or Work Map.
